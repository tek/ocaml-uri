load("@obazl_rules_ocaml//dsl:library.bzl", "lib", "sig")

lib(
    name = "uri-sexp",
    deps_opam = [
        "sexplib",
        "ppx_sexp_conv",
    ],
    modules = dict(uri_sexp = sig()),
    ppx = ["ppx_sexp_conv"],
    deps = ["//lib:#Uri"],
)
