load("//:bzl/library.bzl", "simple_lib")

simple_lib(
    name = "lib-uri_sexp",
    modules = ["uri_sexp"],
    deps_opam = ["sexplib", "ppx_sexp_conv"],
    deps = ["//lib:lib-uri"],
    ppx=True,
)
