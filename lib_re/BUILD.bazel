load("@obazl_rules_ocaml//dsl:library.bzl", "lib", "sig")

lib(
    name = "uri-re",
    deps_opam = [
        "stringext",
        "re",
    ],
    modules = dict(
        uri_legacy = sig(),
        uri_re = sig(),
    ),
)
