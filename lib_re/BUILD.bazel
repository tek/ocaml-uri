load("//:bzl/library.bzl", "simple_lib")

simple_lib(
    name = "lib-uri_re",
    deps_opam = [
        "stringext",
        "re",
    ],
    modules = [
        "uri_re",
        "uri_legacy",
    ],
)
